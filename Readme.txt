Method 1 (108.3 MW): produced by exergy_calculator_v2.py, which opens the validated model, solves it once, evaluates the per-section and plant-level exergy balance, and writes the results to exergy_results_v2.xlsx. The underlying converged stream/block states are the Aspen Plus export in New_Microsoft_Excel_Worksheet.xlsx.
Method 2 (~110 MW): produced by exergy_campaign_driver.py, which generates the ±10% sampling campaign (reinitialise → write inputs → solve → evaluate) and writes ann_dataset.csv; the base-point value is the dataset rows nearest the base operating condition.